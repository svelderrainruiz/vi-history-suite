name: Public Windows Installed-User Contract

on:
  workflow_dispatch:
  push:
    branches:
      - develop
      - main
      - 'release/**'
      - 'hotfix/**'
    paths:
      - '.devcontainer/**'
      - '.github/workflows/public-source-package-preview.yml'
      - '.github/workflows/public-linux-installed-user-smoke.yml'
      - '.github/workflows/public-windows-installed-user-contract.yml'
      - '.vscode/**'
      - 'README.md'
      - 'INSTALL.md'
      - 'SUPPORT.md'
      - 'CONTRIBUTING.md'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - 'tsconfig.integration.json'
      - 'vitest.config.ts'
      - 'resources/**'
      - 'scripts/**'
      - 'tests/**'
      - 'src/**'
  pull_request:
    branches:
      - develop
      - main
    paths:
      - '.devcontainer/**'
      - '.github/workflows/public-source-package-preview.yml'
      - '.github/workflows/public-linux-installed-user-smoke.yml'
      - '.github/workflows/public-windows-installed-user-contract.yml'
      - '.vscode/**'
      - 'README.md'
      - 'INSTALL.md'
      - 'SUPPORT.md'
      - 'CONTRIBUTING.md'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - 'tsconfig.integration.json'
      - 'vitest.config.ts'
      - 'resources/**'
      - 'scripts/**'
      - 'tests/**'
      - 'src/**'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  public-windows-installed-user-contract:
    runs-on: windows-2022
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run public Windows installed-user contract
        run: npm run public:contract:windows-installed-user -- --evidence-dir artifacts/public-windows-installed-user-contract

      - name: Upload contract evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: public-windows-installed-user-contract
          path: artifacts/public-windows-installed-user-contract/**
          if-no-files-found: warn
